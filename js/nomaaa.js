localStorage. clear();
 // Reset number of hits.
 if( localStorage. booh ){
 localStorage. booh = Number(localStorage. booh) +10;
 }else{
 localStorage. booh = 0;
 }
 document. write("mahits zetu :" + localStorage. booh );